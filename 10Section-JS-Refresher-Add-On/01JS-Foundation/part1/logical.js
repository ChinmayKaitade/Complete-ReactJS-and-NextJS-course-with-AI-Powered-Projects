// && -> and
// || -> or
// ! -> reverse

let isLoggedIn = true;
let isPaid = true;

console.log("isLoggedIn && isPaid:", isLoggedIn && isPaid);

let isEmailUser = true;
let isGoogleUser = false;

console.log("isEmailUser || isGoogleUser:", isEmailUser || isGoogleUser);
